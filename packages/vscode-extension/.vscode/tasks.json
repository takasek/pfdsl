{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build",
			"detail": "Build extension + its @pfdsl/* deps so the Dev Host loads fresh dist",
			"type": "shell",
			"command": "make vscode-build",
			"options": { "cwd": "${workspaceFolder}/../.." },
			"group": "build",
			"problemMatcher": []
		}
	]
}
