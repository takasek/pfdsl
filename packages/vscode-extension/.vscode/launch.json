{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Run PFDSL Extension",
			"type": "extensionHost",
			"request": "launch",
			"args": ["--extensionDevelopmentPath=${workspaceFolder}"],
			"outFiles": ["${workspaceFolder}/dist/**/*.cjs"],
			"preLaunchTask": "build"
		}
	]
}
