# deps
node_modules/

# build
dist/
*.tsbuildinfo

# test
coverage/

# env
.env
.env.*
!.env.example

# OS
.DS_Store

# editor
.vscode/
# but keep the extension's debug launch config (deterministic F5 in any worktree)
!packages/vscode-extension/.vscode/
!packages/vscode-extension/.vscode/launch.json
!packages/vscode-extension/.vscode/tasks.json
.idea/

# pnpm
.pnpm-store/

# claude
.claude/settings.local.json
.claude/worktrees/
.superpowers/
# symlink to the tracked bundle copy plugin/pfdsl/skills/pfdsl, created by
# 'make setup' (#348 dropped the tracked snapshot, #714 replaced the local
# generated copy with this link)
.claude/skills/pfdsl

# worktrees
.worktrees/

# PR diff images (pushed to ci-image-store instead)
docs/diagrams/

# vscode extension package artifacts
*.vsix
