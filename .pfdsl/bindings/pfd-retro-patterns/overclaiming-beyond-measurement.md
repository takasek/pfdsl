---
tags: [always]
---

- **測った水準を超えて書く**: 実測で確かめたのは挙動の一致なのに、成果物には実装内部が同一であるかのように書いてしまうことがある。
  一段強い主張のほうが読み手に届く説明になるため、書いている側は補強のつもりでいる。
  コメントや設計記録に残ると、後続の読み手はその強い主張を前提に検証を省く。根拠の水準が落ちたことは文面からは見えない。
  問いの形: 「この1文が主張している水準は、実際に走らせて確かめた水準と同じか」。
  具体例: glob 判定を Node の API へ委譲した際、実測したのは `fs.globSync` との**挙動の一致**だったが、JSDoc には「`node --test` が引数解決に使うのと同じエンジン」と書いた（#582。実装内部は確認していない。検証した内容の記述へ差し替えた）。
  具体例: 同じ実測を2通りに書き、片方だけ分類フィルタを落とした形もある。#725 の設計選択記録は「消費者を持たない版 artifact は1件」と書いたが、`graph io` の `terminals` を機械的に数えると6件で、1件は「起こす契機のどちらにも当たらないもの」に絞った後の値だった。同じコメントの次の項目には絞る前の値と絞った理由が正しく書かれており、**前の行へ結論だけを転記する過程で単位が落ちた**。gate-check の design-selection record は書式しか見ないため PASS のまま通り、PR 本文にも同じ圧縮が伝播した（PR #865。サイクル後の敵対的検証で検出）。
  具体例: PR #1036 のレビューサイクルで「実装完了と判定」と報告したが、自分で走らせたのは strict check、edge 数、grep によるエッジ実在確認、checker テスト51件だけで、`make check-docs`、全テスト、build は委譲先の申告のまま判定へ含めていた。retro で `make check-docs` を走らせ直すと exit 0 だったが、結論が正しかったことは、判定時点で測っていない範囲まで自分の実測として扱ったことを正当化しない（#1037 で記録）。
  **強い語が入っていなくても起きる。** 数字は語より強い主張に見えるうえ、単位が文言から落ちても数字自体は変わらないため、読み手にも書き手にも欠落が見えない。
  **表の見出しが行ごとの根拠水準を平らにする形もある。** 「実測」と題した表に、実際に確かめた行と一次情報からの推測でしかない行が同じ列に並ぶと、水準の差は列の中に表現される場所を持たない。
  作業項目が完了済みで実体を辿れる行と、未着手で実体がまだ存在しない行は、同じ表に並べれば同じ強さで読まれる。
  対策の追加: 表を「実測」と題する前に、各行について確かめた手段（開いたファイル・走らせたコマンド）を1つ挙げられるか行単位で問う。挙げられない行は表から外すか、その行だけ根拠の水準を明記する。
  対策: 主張を書いたら、その根拠として走らせたコマンドを1つ思い出せるか確認する。思い出せない語（同一・常に・原理的に等）が入っていたら、測った内容の記述へ落とす。
