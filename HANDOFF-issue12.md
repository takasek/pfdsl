# 引き継ぎ: issue #12 — PFDSL 公開記事（連載）の骨子を詰めて書く

外部接続が許可された環境の次セッション向けの引き継ぎ。この1枚で文脈が分かるように書いてある。
**まだ「骨子（構成案）」の段階。ユーザー未承認。いきなり本文を書かず、下記を読んで骨子を詰めてから書くこと。**

---

## 1. タスク

issue #12「`docs/adr/` の設計決定記録（ADR）を素材に、PFDSL の問題意識と設計根拠を説明する
公開記事を書く」。リポの生態系図上は `write_article` プロセス（最終納品物 `article`）にあたる。
※issue 本文は古い（執筆当時 ADR 6 本・仕様 v0.0.7 想定）。現状は ADR 22 本・仕様 v0.0.8。本文は読み替える。

## 2. 背景（ユーザーと PFDSL）

- PFDSL は、清水吉男氏の PFD（Process Flow Diagram＝プロセスと成果物の依存をグラフで描く記法）を
  下敷きにした、テキストベースの DSL とそのツール群。リポ: takasek/pfdsl。
- ユーザーは**所属会社（現職）**で PFD のツール化に取り組んできた。**社内ツールは GUI ツール**。
- 参考ブログ（SWET。これまでの取り組みの記録で、**記事の主題でも比較対象でもなく背景の参考**）:
  - https://swet.dena.com/entry/2024/11/15/120000
  - https://swet.dena.com/entry/2026/01/16/120000

## 3. 最初に読むもの（骨子を詰める前に）

1. 上記ブログ2本（外部接続が要る。背景把握用）。
2. リポ内の素材: `docs/adr/`（特に 0001〜0006 が言語設計の核）、`docs/pfd_payoff_log.md`
   （PFD が効いた実エピソード集）、`README.md`、`docs/spec/spec.md`。
3. スキル: `pfd-ops`（運用プロトコル／pfd-cycle）と `pfdsl`（.pfdsl の記法・check）をロードして従う。

## 4. 記事の軸（最重要。ユーザーの明示指示）

**書くこと:**
- **そもそもツール自体がない状態での問題**。プロセスを形式化したいが、それを支え・制約を守らせる
  手段が存在しない、という状況そのものの問題。
- その**解としての PFDSL**。
- **制約をツールで管理することは重要な要求**（形式化は規約・人手レビューだけでは形骸化する。
  単一生成元・DAG 性・終端健全性などを機械が検査できる必要がある）。

**書かないこと（明示的な NG）:**
- 社内 GUI ツールとの差分・比較を主題にしない。社内ツールもブログも背景の参考にすぎない。
- 「作図ツールの限界」を記事の中核に据えない。
- AI 臭い・ポエム調・過剰な比喩・情緒的レトリックを使わない。**淡々と具体的に書く**。

## 5. これまでの問答の経緯（提案 → ユーザーの応答）

骨子は何度かの往復で形を変えてきた。確定/未確定が分かるように、実際のやりとりをそのまま残す。
次セッションは「最新の応答」を優先しつつ、棄却された案を再提案しないこと。

1. **最初の計画案（アシスタント）**: 単発記事で ADR-0001〜0006 を背骨に「清水 PFD から何を残し何を
   変えたか」を書く。
   → **ユーザー**: 「もっと抽象度の高いところから。読者は誰で、何を理解してほしい記事なのか。
   必要なら連載にしてスコープを絞れ」。

2. **読者・テーゼ・形態を質問（アシスタント）**: 3択で確認。
   → **ユーザーの回答**: 読者＝プロセス設計に関心のある一般エンジニア／テーゼ＝成果物中心の実用価値／
   形態＝テーマを絞った連載。
   （※読者については後の 6 でさらに「狭すぎる」と修正が入る。下記参照）

3. **価値先行の連載構成を提案（アシスタント）**: 第1部＝成果物中心モデルの効能（計画/進捗/レビューの
   機械化）、第2部＝それを支える制約、第3部＝dogfooding、という分割。
   → **ユーザー（大きな方向転換）**: 「まず PFDSL を作るに至った**問題意識**と、**何を解決するのか**を
   焦点に」。あわせて背景を提示——所属会社で PFD ツール化に取り組んだ／社内ツールは GUI／
   プロセスの形式化は大事だが**制約を強制する手段がないのでツールが要る**／AI との意思疎通言語に
   するには**テキストベース**でやりたい／**GUI はだるい**／座標情報は要らず**グラフだけ端的に編集**したい／
   **メタデータも大事で柔軟性が欲しい**。
   さらに「第1部で『ある設計判断』と**もったいぶるな**。読み物としてのヒキではなく、細部に入りすぎると
   ブレるから**分けるだけ**にしろ」。

4. **問題意識の深掘りを質問（アシスタント）**: ブログを読めない環境だったため、(a)「GUI ツールから
   DSL に移るとき『だから DSL が要る』と思わせた核心の躓きは何か」、(b)「AI 意思疎通言語という角度の
   比重」を質問。
   → **ユーザーの回答**: (a) **git で diff/レビューできない・GUI の編集コストが高い・AI に渡せない**
   （「図が形骸化した」は選ばれなかった）。(b) **動機の一つとして触れる程度**（主軸ではない）。
   ※この3点は上記の質問への回答という文脈で出たもの。**GUI→DSL 移行時の問題意識の材料**であって、
   記事の軸（§4「ツール不在の問題」）とは別物。記事を GUI ツール叩きにしないこと。

5. **第1部ドラフトを執筆・コミット（アシスタント）**: 問題先行で執筆。
   → **ユーザー**: 「AI 臭いポエムをやめろ」「plan は（ツールの都合で）抜けたが**承認はしていない／
   骨子はまだ詰まっていない**」。加えて事実の訂正——**前職ではなく所属会社**／URL はブログ本文ではなく
   **補足**／**社内ツールは GUI**／この記事は**社内ツールとの差分にフォーカスするものではない**／
   書きたいのは**ツール自体がない状態での問題と、その解としての PFDSL**で、その意味で**制約をツールで
   管理するのは重要な要求**。「記事を読めないなら話にならないので、外部接続環境に引き継げ」。
   → ドラフトと flow 変更は**撤回済み**（§9）。

## 6. 編集方針（確定/修正を反映）

- **読者**: エンジニア一般。「プロセス設計に関心のある人」に**限定しない**——プロセス（仕事の段取り）は
  職種を問わず誰もが日々扱うものなので、**間口は広く取る**。PFD の前提知識は仮定せず導入から書く。
  （当初「プロセス設計に関心のある一般エンジニア」としたが、ユーザーが「狭すぎる」と修正）。
- **言語**: 日本語（CLAUDE.md「公開ドキュメントは英語」への意図的例外。対象が日本の方法論で
  読者・媒体が日本語のため、ユーザーが明示選択）。
- **形態**: テーマを絞った連載（1記事1テーゼ）。分割は焦点維持のためで、ヒキ／もったいつけ目的ではない。
- **第1部の焦点**: §4 の軸（ツール不在の問題 → 解としての PFDSL）。

## 7. 連載の暫定構成（未確定。ユーザーと詰める）

- 第1部: ツール不在の問題 →（制約のツール管理を含む）要求の導出 → 解としての PFDSL。
- 第2部（暫定）: 各制約をなぜその形にしたか（単一生成元・成果物の有形性・update 不採用・
  条件分岐不採用・プロセス粒度 ＝ ADR-0001〜0005）の設計判断。
- 第3部（任意）: 方法論を自分自身に適用する dogfooding／AI 協働のメタ。

※連載の本数・各部のテーゼ・第1部の節構成は未確定。骨子としてユーザーと合意してから書く。

## 8. まだ詰める骨子の問い（次セッションでユーザーと）

1. 「ツール不在の問題」を読者にどう具体化するか（共感を呼ぶ入口・失敗の像）。
2. 「制約をツールで管理する」要求を、どう腹落ちさせるか（具体例・形骸化の像）。
3. 第1部で使う `payoff_log` のエピソード選定（効能の裏づけ）。
4. 連載の本数と各部の責務の最終確定。
5. ブログ2本を本文で参照するか／末尾の参考に留めるか。

## 9. リポの現状

- ブランチ: `claude/issue-12-progress-049pqo`（このタスク用。main 直コミット禁止）。
- 一度コミットした記事ドラフトと flow 変更は**撤回済み**（force-push でクリーン化）。
  `article`/`i12_article` は `todo` のまま。ゼロから骨子→執筆を始められる。
- このブランチに置いてあるのは本引き継ぎ文書（`HANDOFF-issue12.md`）のみ。

## 10. 運用・規約

- pfd-ops の pfd-cycle に従う（1サイクル=1プロセス＝`write_article`）。
- コミット: 論理単位ごと、Conventional Commits、メッセージは英語。
- 記事は `docs/article/` 配下に置く想定（新規。連載なのでディレクトリ）。
- `.pfdsl` を触ったら `node packages/cli/dist/cli.js check <file>`（要 `pnpm install && pnpm -r build`）。
  `make setup` で pre-commit hook（biome 整形・snapshot 鮮度・gen-skill 鮮度を検査）を導入。
- 記事の ```pfdsl コードブロックは `node scripts/check-doc-examples.mjs <md>` で検証できる。
- タイミング規約: `article` の status→done と issue close は **main への PR マージ時**。公開（外部URL取得）は
  ユーザー作業で、公開後に done。feature branch では `closes #12` を使わない。
- PR はユーザーが明示依頼するまで作らない。

## 11. 進め方（次の一歩）

1. §3 を読む。2. §4 の軸と §5 の経緯を踏まえ、§8 の問いをユーザーと詰めて骨子を確定。
3. 骨子合意後に第1部を執筆。4. 文体は淡々と具体的に（§4 の NG を厳守）。
