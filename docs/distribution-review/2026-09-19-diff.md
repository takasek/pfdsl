# 2026-09-19 差分モード — PR-B（#1211）の配布散文縮小

base: `fbb611d621fdfdefba3490062ff6bf674b992bb3`（前回承認 commit）。
対象: 24ファイル。`runDistributionReviewCheck` の出力をそのまま使った。
このレビューは #1211 の「検証（採用側）」を兼ねる。採用側検証としての証拠は PR 本文にも写す。

## 組んだ課題

差分が触った能力ごとに1課題1 subagent。sandbox は `/tmp/adopt-1211-782486-*` と `/tmp/pfdsl-probe-*`。

| probe | 課題 | 触った差分 |
|---|---|---|
| filebased | file-based バックエンドで1サイクル完走（着手可能集合の導出・status 更新・完了契約）と、D 層の採用行の有無での切替 | work-cycle.md の縮小、pfd-retro の D 層 opt-in |
| gh | 分割後の L3 契約と検証用 binding だけで、設計記録の配置・正本の同定・書式・再承認が成立するか判定 | github-issues-backend.md「設計確定の証拠」の分割 |
| promotion | companion にある汎用ルールらしき4件の昇格先を決める | architecture.md の0段目、pfd-retro の L4 滞留監査 |
| nobinding | scaffold のままのリポで1サイクル開始する | work-cycle.md の手順骨格、binding の追加手順への委譲、scaffold |

`filebased` と `gh` は `plugin/pfdsl` の写しと利用側 binding だけを渡した。
`promotion` と `nobinding` はスキルの手順に従い `repo/` + `plugin-cache/pfdsl/` の構成で、CLI は npm 公開版 `@pfdsl/cli@0.0.26` を入れた（公開版と作業ツリーのビルドは同じ 0.0.26 を名乗るため、`release-status` が `commits-ahead` を報告していることも確認した）。

## 各 probe の詰まりと分類

### (a) 配布プロンプトの欠陥 — 4件、いずれも canonical 側を修正した

1. **0段目が採用リポから到達できない文書を一次情報に指していた**（`promotion`）。
   `architecture.md`「昇格先の判定ルール」に足した0段目が「区分の一次情報は ADR-0039」と書いていた。
   同じファイルの「配布物中の ADR 参照の解決」は `ADR-\d+` が上流リポにしか無いと定めているため、probe は昇格判定の最も効く条件をその一段落の要約からしか適用できなかった。
   報告の文言: 「it means I worked from a summary, not the primary source, for the single most load-bearing test in the whole decision.」
   修正: 3区分の定義を0段目に inline し、この段の判定はそこで閉じると明記。ADR-0039 は経緯の記録へ格下げ（commit `02b7eb65`）。

2. **scaffold の `roadmap.md` に宛先を失った記入欄が残っていた**（`nobinding`）。
   open PR の確認は区分 iii として binding へ移ったのに、scaffold は引き続き `## Open PR（ワークサイクル選択前に確認）` の記入を求めていた。
   probe は見出しを読み、bundle 側にそれを読む主体が無いことを確かめ、load-bearing か vestigial か判定できなかった。
   修正: 記入欄を scaffold の `bindings/pfd-ops.md` へ移し、`## ワークサイクルの追加手順` 見出しとその用途の説明を付けた（commit `127dc7fa`）。

3. **手順2の pipeline 判定が、同じファイルの他の箇所と線引きが違っていた**（`nobinding`）。
   「`.pfdsl/pipeline.pfdsl` が存在する場合は…」は、scaffold のままのファイルが「存在する」に当たるかを答えていなかった。
   同じファイルは `workflow.pfdsl`（21行）と終端ゲート（64行）では実在と採用を区別しており、probe はその非対称を指摘した。
   縮小前は長い1行の中に埋もれていた文が、縮小後は手順2の3文のうち1つになったため読み手の判断点として前に出た。
   修正: 採用済み（scaffold でない）を条件にし、判定は前提条件の scaffold 判定を使うと明記（commit `127dc7fa`）。

4. **D 層の採用宣言に対象の列挙が無い状態の扱いが無かった**（`filebased`）。
   probe は採用行を足した後、`references/knowledge-lifecycle.md` が「対象はその binding が列挙する成果物に限る」と narrow するのに列挙が無く、何をすべきか書かれていないと報告した。
   あわせて、その binding には `## D. 知識成果物のライフサイクル（追加パターン）` という**見出し**が既にあり、それは採用宣言を満たさないことを確かめている。
   修正: 列挙が無い場合は監査対象を推測せず、欠けている旨を報告して適用しないと明記。見出しの一致が宣言の代わりにならないことも書いた（commit `4445a373`）。

### (c) 環境制約 — 配布物は直さない

- **`main-commit-guard.mjs` が sandbox の `main` で発火した**（`filebased`）。
  このリポの PreToolUse hook が、対象リポジトリを問わずブランチ名 `main` で一致した。
  probe は sandbox 内に `probe/t1-cycle` を切って回避した。
  配布物の欠陥ではなく、このリポの hook の作用域の問題。別 issue の候補として切り出した。
- **`pfdsl status ready` が scaffold のプレースホルダを best として返す**（`nobinding`）。
  出力は `{"best":{"id":"start_work","label":"(最初の作業プロセスに置き換える)"}}`。
  scaffold 判定は散文の grep だけが持ち、CLI は持たない。本 PR の変更とは独立の既存の食い違い。
- **`pfd-ecosystem` の問診が対話前提で、非対話の probe では実行できない**（`nobinding`）。課題設計側の制約でもある。
- **scaffold のまま「本気で使いたい」が pfd-ecosystem 案内の「明示的な依頼」に当たるかの判断基準が無い**（`nobinding`）。
  前提条件は scaffold 判定には機械的なコマンドを与えているが、この判断には与えていない。本 PR が触っていない既存の非対称。

## 越境の事後検出

4 probe の報告に、このリポの絶対パス・`plugin/pfdsl/...` の相対形・`.claude/skills/pfd-*` の実体を読んだ形跡はいずれも無い。
`nobinding` は `ls .claude` が存在しないことを報告しており、`SKILL.md` の repo-local fallback が plugin 経由の採用リポでは空振りすることを利用側の視点から確かめている。
`gh` probe は GitHub への書き込みを求められた箇所で停止し、投稿予定の本文を報告に置いた（書き込みは呼び出し元が行った）。

## 詰まりが消えたことの確認（手順8）

修正後の bundle を新しい sandbox へ入れ、同じ課題を回し直した。

- `promotion`（再: `/tmp/adopt-1211-782486-promotion3`）: **stalls none**。報告の文言: 「I could apply the test fully with only bundle-local files; the unreachable ADR-0039 did not block me.」
  肯定側の宛先として scaffold の `bindings/pfd-ops.md` に足した案内行を引いており、修正2も同時に確認できた。
- `nobinding`（再: `/tmp/adopt-1211-782486-nobinding2`）: 手順2の詰まりが消えた。報告の文言: 「this was the one determination in the whole exercise that was fully mechanical and unambiguous. I did not have to guess here.」
  上の (c) の4件は残っている（配布物を直さないと決めたもの）。
- `filebased` / `gh` の修正（4と、配置契約の4文）は再実行していない。修正4は probe が観測した欠落そのものに文を足したもので、同じ probe を回し直す前に session limit に当たった。未了として PR 本文に明示する。

## 教訓への昇格

`docs/distribution-review.md` へ次の1件を昇格した。
**散文を減らす変更は、残った文の読まれ方を変える。** 長い1行に埋もれていた条件が、縮小後は数文のうちの1つになり読み手の判断点として前に出る。
移設した文だけでなく、残した文のうち移設で相対的な重みが変わったものも probe の対象に含める。
（この差分では手順2の pipeline 判定がその形で出た。文自体は無変更で、机上のレビューでは差分に現れない。）

## 記録

- 承認 commit: `0fe24770718901ed8cadf7e452ed783c006847bc`
- 対象ファイル数: 24（`plugin/pfdsl` 12・`plugin/pfdsl-codex` 12）
