# 2026-09-19 差分モード — PR-B（#1211）の配布散文縮小

base: `fbb611d621fdfdefba3490062ff6bf674b992bb3`（前回承認 commit）。
対象: 24ファイル。`runDistributionReviewCheck` の出力をそのまま使った。
このレビューは #1211 の「検証（採用側）」を兼ねる。採用側検証としての証拠は PR 本文にも写す。

## 組んだ課題

差分が触った能力ごとに1課題1 subagent。sandbox は `/tmp/adopt-1211-782486-*` と `/tmp/pfdsl-probe-*`。

| probe | 課題 | 触った差分 |
|---|---|---|
| filebased | file-based バックエンドで1サイクル完走（着手可能集合の導出・status 更新・完了契約）と、D 層の採用行の有無での切替 | work-cycle.md の縮小、pfd-retro の D 層 opt-in |
| gh | 分割後の L3 契約と検証用 binding だけで、設計記録の配置・正本の同定・書式・再承認が成立するか判定 | github-issues-backend.md「設計確定の証拠」の分割 |
| promotion | companion にある汎用ルールらしき4件の昇格先を決める | architecture.md の0段目、pfd-retro の L4 滞留監査 |
| nobinding | scaffold のままのリポで1サイクル開始する | work-cycle.md の手順骨格、binding の追加手順への委譲、scaffold |

`filebased` と `gh` は `plugin/pfdsl` の写しと利用側 binding だけを渡した。
`promotion` と `nobinding` はスキルの手順に従い `repo/` + `plugin-cache/pfdsl/` の構成で、CLI は npm 公開版 `@pfdsl/cli@0.0.26` を入れた（公開版と作業ツリーのビルドは同じ 0.0.26 を名乗るため、`release-status` が `commits-ahead` を報告していることも確認した）。

## 各 probe の詰まりと分類

### (a) 配布プロンプトの欠陥 — 5件、いずれも canonical 側を修正した

1. **0段目が採用リポから到達できない文書を一次情報に指していた**（`promotion`）。
   `architecture.md`「昇格先の判定ルール」に足した0段目が「区分の一次情報は ADR-0039」と書いていた。
   同じファイルの「配布物中の ADR 参照の解決」は `ADR-\d+` が上流リポにしか無いと定めているため、probe は昇格判定の最も効く条件をその一段落の要約からしか適用できなかった。
   報告の文言: 「it means I worked from a summary, not the primary source, for the single most load-bearing test in the whole decision.」
   修正: 3区分の定義を0段目に inline し、この段の判定はそこで閉じると明記。ADR-0039 は経緯の記録へ格下げ（commit `02b7eb65`）。

2. **scaffold の `roadmap.md` に宛先を失った記入欄が残っていた**（`nobinding`）。
   open PR の確認は区分 iii として binding へ移ったのに、scaffold は引き続き `## Open PR（ワークサイクル選択前に確認）` の記入を求めていた。
   probe は見出しを読み、bundle 側にそれを読む主体が無いことを確かめ、load-bearing か vestigial か判定できなかった。
   修正: 記入欄を scaffold の `bindings/pfd-ops.md` へ移し、`## ワークサイクルの追加手順` 見出しとその用途の説明を付けた（commit `127dc7fa`）。

3. **手順2の pipeline 判定が、同じファイルの他の箇所と線引きが違っていた**（`nobinding`）。
   「`.pfdsl/pipeline.pfdsl` が存在する場合は…」は、scaffold のままのファイルが「存在する」に当たるかを答えていなかった。
   同じファイルは `workflow.pfdsl`（21行）と終端ゲート（64行）では実在と採用を区別しており、probe はその非対称を指摘した。
   縮小前は長い1行の中に埋もれていた文が、縮小後は手順2の3文のうち1つになったため読み手の判断点として前に出た。
   修正: 採用済み（scaffold でない）を条件にし、判定は前提条件の scaffold 判定を使うと明記（commit `127dc7fa`）。

4. **D 層の採用宣言に対象の列挙が無い状態の扱いが無かった**（`filebased`）。
   probe は採用行を足した後、`references/knowledge-lifecycle.md` が「対象はその binding が列挙する成果物に限る」と narrow するのに列挙が無く、何をすべきか書かれていないと報告した。
   あわせて、その binding には `## D. 知識成果物のライフサイクル（追加パターン）` という**見出し**が既にあり、それは採用宣言を満たさないことを確かめている。
   修正: 列挙が無い場合は監査対象を推測せず、欠けている旨を報告して適用しないと明記。見出しの一致が宣言の代わりにならないことも書いた（commit `4445a373`）。

5. **scaffold の実データ化依頼と判別できないときの行き先が無かった**（`nobinding`、当初 (c) として記録したものを所有者の判断で (a) へ再分類した）。
   前提条件は scaffold 判定には機械的なコマンドを与えるが、「ユーザーが明示的に依頼したか」には与えていない。
   probe の報告: 「the bundle gives no threshold for how much inferred intent counts as「明示的」… This is a judgement call a differently-calibrated reader could make the other way.」
   読み手が自分で決めると「案内しない」側にも「勝手に実データ化する」側にも倒れうる。
   修正: 判別できない場合はどちらとも決めずにユーザーへ問い、問いが解けるまで非適用のままとする。

   **この修正は最初 `work-cycle.md`「前提条件」に入れたが、再プローブで読まれないことが判明した。**
   probe は `pfd-ops/SKILL.md`「適用単位」で同じ文に当たってそこで止まり、`work-cycle.md` の前提条件へは到達しなかった。
   同じ規則が `pfd-ops/SKILL.md`・`work-cycle.md`・`pfd-retro/SKILL.md` の3箇所にあり、読み手が最初に踏むのは1つめである。
   解決を `pfd-ops/SKILL.md`「適用単位」へ置き、他の2箇所はそこを指すポインタにした。
   移設後の再プローブ（`/tmp/adopt-1211-782486-nobinding4`）で詰まりが消えた — probe は `pfd-ops/SKILL.md`「適用単位」の追加文を引き、どちらとも決めずに問いを呼び出し元へ返した。`other stalls: none`。

### (c) 環境制約 — 配布物は直さない

- **`main-commit-guard.mjs` が sandbox の `main` で発火した**（`filebased`）。
  このリポの PreToolUse hook が、対象リポジトリを問わずブランチ名 `main` で一致した。
  probe は sandbox 内に `probe/t1-cycle` を切って回避した。
  配布物の欠陥ではなく、このリポの hook の作用域の問題。別 issue の候補として切り出した。
- **`pfdsl status ready` が scaffold のプレースホルダを best として返す**（`nobinding`）。
  出力は `{"best":{"id":"start_work","label":"(最初の作業プロセスに置き換える)"}}`。
  scaffold 判定は散文の grep だけが持ち、CLI は持たない。本 PR の変更とは独立の既存の食い違い。
- **`pfd-ecosystem` の問診が対話前提で、非対話の probe では実行できない**（`nobinding`）。課題設計側の制約でもある。

## 越境の事後検出

4 probe の報告に、このリポの絶対パス・`plugin/pfdsl/...` の相対形・`.claude/skills/pfd-*` の実体を読んだ形跡はいずれも無い。
`nobinding` は `ls .claude` が存在しないことを報告しており、`SKILL.md` の repo-local fallback が plugin 経由の採用リポでは空振りすることを利用側の視点から確かめている。
`gh` probe は GitHub への書き込みを求められた箇所で停止し、投稿予定の本文を報告に置いた（書き込みは呼び出し元が行った）。

## 詰まりが消えたことの確認（手順8）

修正後の bundle を新しい sandbox へ入れ、同じ課題を回し直した。

- `promotion`（再: `/tmp/adopt-1211-782486-promotion3`）: **stalls none**。報告の文言: 「I could apply the test fully with only bundle-local files; the unreachable ADR-0039 did not block me.」
  肯定側の宛先として scaffold の `bindings/pfd-ops.md` に足した案内行を引いており、修正2も同時に確認できた。
- `nobinding`（再: `/tmp/adopt-1211-782486-nobinding2`）: 手順2の詰まりが消えた。報告の文言: 「this was the one determination in the whole exercise that was fully mechanical and unambiguous. I did not have to guess here.」
  上の (c) の3件は残っている（配布物を直さないと決めたもの）。
- `filebased` / `gh` の修正（4と、配置契約の4文）は再実行していない。修正4は probe が観測した欠落そのものに文を足したもので、同じ probe を回し直す前に session limit に当たった。未了として PR 本文に明示する。

## 教訓への昇格

`docs/distribution-review.md` へ次の1件を昇格した。
**散文を減らす変更は、残った文の読まれ方を変える。** 長い1行に埋もれていた条件が、縮小後は数文のうちの1つになり読み手の判断点として前に出る。
移設した文だけでなく、残した文のうち移設で相対的な重みが変わったものも probe の対象に含める。
（この差分では手順2の pipeline 判定がその形で出た。文自体は無変更で、机上のレビューでは差分に現れない。）

## 追記（同日、修正後）

上の (a) 5件のうち4と5は、probe が観測した欠落そのものに文を足したものである。
4（D 層の列挙欠落）は再プローブしていない — 未了として PR 本文に明示した。
5 は再プローブで宛先の誤りが出たため、解決の置き場を変えて再度確認し、詰まりが消えたことを確かめた。

教訓へ1件追加した。
**同じ規則が複数の層にあるとき、修正は読み手が最初に踏む層へ置く。**
規則の重複自体は既知の型（列挙ドリフト）だが、修正の宛先を間違える形はそれとは別に現れる — 直した文が読まれないので、probe の報告は修正前と同じ詰まりになり、修正が効いていないのか宛先が違うのかが報告からは区別できない。
再プローブの報告が「どのファイルのどの節を読んだか」を含んでいたことでこれが見えた。ブリーフに file:section の報告を要求する理由の1つがここにある。

## 第2ラウンド（2026-09-21、独立レビュー起点）

別モデルによる #1219 のレビューが3件を指摘し、いずれも一次情報で成立を確認した。3件とも第1ラウンドの修正が作った欠陥である。

6. **書式の定義を外したのに、書式の要求が残った**（独立レビュー、のち2 probe が独立に到達）。
   `file-based-tracker-backend.md` は書式を binding へ委ねる一方、移行履歴と再承認参照の2箇所で `Format 3` を名前で要求し続けていた。
   採用リポは定義の無い書式の生成を命じられる。file-based の record probe は記録を作れずに停止した。
   **一度目の修正は不十分だった** — 名前を外して「binding に書式が無ければ、記録を作る前に用意する」に替えたところ、再プローブは同じ場所で止まった（「形も欄も例も無く、発明してよいのか人間の判断を待つべきかが決められない」）。行き止まりの文言が変わっただけだった。
   二度目の修正で GitHub 側と対称にした — 記録を要求するかどうか自体を binding に委ね、定めが無ければこのプリセットは記録を要求しない。
   再プローブは「conditional — and for this repo, no」と答え、該当文を引き binding の中身を確かめてサイクルを完走した。
   検査も直した: 旧テストは宣言トークン `設計記録形式: 3` の不在だけを見ており、要求の残存を見ていなかった。書式の名前と無条件要求の2変異で落ちることを確認した。

7. **発火時の読込を絞った結果、binding の6節が入口を失った**（独立レビュー）。
   第1ラウンドの D 層スケール対策（全読を避ける）が到達性の退行とトレードになっていた。
   失われた中に「CLI はローカルビルドを叩く」があり、その欠落は公開版で main 先行のリポを検査する誤判定を許す。
   修正: 発火時に見出し一覧（455 bytes、全体 65,995 bytes）を毎回取り、主題が該当する節の本文を読む。採用リポは binding に任意の節を置けるので配布層は一覧を持てず、索引を読む以外にない。
   再プローブは見出し一覧から2節を見つけて従い、ローカル CLI を選んだ理由に binding を挙げた。

8. **pfd-retro の実行契約に、収集前に読む入口が無かった**（独立レビュー）。
   移設前は SKILL.md 内の順序で読まれていた。移設後は入口が無く、契約が要求する「収集前の実行 ID・cutoff の確定」を過ぎてから見つかる。
   修正: 冒頭に、収集開始前に binding の見出しを見て実行契約に従う旨を置いた。
   **このサイクルの retro 自身がこれを踏んでいた** — 実行 ID も cutoff も決めずに A/B の機械列挙から始めた。

9. **自己点検の出力について、散文が実挙動と食い違っていた**（2 probe が独立に到達）。
   `architecture.md` は「GitHub Issues バックエンド未採用のリポでは何も出ない」と書くが、`--target` を非採用リポへ向けて実行すると未採用の通知と `--deploy` の案内の2行が出る。
   本 PR が作った欠陥ではないが、2経路からの収束であり、probe はどちらも「無視してよいのか設定が古いのか判定できない」と報告した。
   修正: 実挙動を記述し、その行は報告であって指示ではないこと、別バックエンドを採用しているリポは従わないことを明記。検査で記述を挙動に固定した。

### 第2ラウンドの (c)

- **`check-md-linebreaks` の PostToolUse hook が sandbox の `work-items.md` に発火した**（entry-recheck probe）。
  この形式は1行1フィールドで散文ではない。`main-commit-guard` と同じ作用域の問題で、#1221 へ追記した。
- **`${CLAUDE_PLUGIN_ROOT}` が置換されたかの判定手段が、ファイル直読みモードでは示されていない**（fb-recheck2 probe）。
  probe は第3フォールバック（読んでいるファイルの所在から相対）で解決したが、本文だけを渡された読み手には解決先が無いと指摘した。
  distribution-review の隔離要件（Skill tool を使わずファイルを直読みさせる）が作る条件でもあるため、配布物は直さず記録に残す。

## 第3ラウンド（2026-09-21、観点3(b) の指摘起点）

10. **再承認参照の扱いが2 backend で非対称だった**（独立レビュー）。
    `file-based-tracker-backend.md` は再承認参照を「`コミット` + 半角スペース + 40桁 SHA」に固定し、`承認履歴:` への追記と承認・改訂の2コミット分割を規定していた。
    一方 GitHub 側は変更2で canonical comment URL の文法を binding へ移しており、同じ種類の規則が片方だけ配布層に残っていた。
    独自の承認リンクを定めた binding で probe すると両方を満たせず停止した。
    **この非対称は第1ラウンドの変更2が作ったもの** — GitHub 側だけ移し、file-based 側を見ていなかった。
    所有者の判断で読み A を採った: backend の事実（ファイルベースではコミット SHA で特定の版を指せる）と、そこから導けない規律（参照は SHA に限る・2コミットに分ける・`承認履歴:` を使う）を分け、後者を区分 iii として L3 から外す。
    何を承認証跡とするかは採用リポの設計であり、SHA が唯一安定な識別子とも言えない。
    このリポは file-based を採用していないので移設先は無く、運用レシピが配布から減るのは責務縮小の意図した結果である。
    再プローブ（`/tmp/adopt-782486-fb-r3`、binding が署名付きタグを承認証跡に定める構成）は **stalls none** で、`再承認: approved-by-takasek-20260921` を binding の規則を根拠に書いた。
    probe の判定: 「the bundle leaving a door open and the binding closing it in a specific way, not two rules disagreeing」。
    検査は値の形の復活・`承認履歴:` の復活・委譲文の消失をそれぞれ拒否する。ベースラインが `fail 0` であることを先に固定してから両変異を当て、赤・復元で緑を確認した（#1223 の3状態の区別を満たす形で判定した）。

### 第3ラウンドの (b)

- **記録を単独コミットにできなかった**（fb-r3 probe）。seed 時に `work-items.md` へ未コミットの status 変更を残していたため、probe の追記を含めると「それだけを単独のコミットにする」を破る。
  probe は強行せず未コミットのまま報告した。課題設計（sandbox の seed）の不備で、配布物の欠陥ではない。

### 報告と実体の差（自己申告の誤り2件）

- **教訓「要求と定義を分離しない」が昇格されていなかった。** 第2ラウンドのコミットメッセージは昇格したと述べたが、昇格の編集は `git add` と同じシェル呼び出しに入っていて guard に拒否され、再試行では編集を落として検査だけを走らせていた。検査はツリーに異常が無いので通り、走らなかった書き込みは検出しない。独立レビューの指摘で判明し、ファイルを読み戻して確認した（教訓 15件 → 16件）。
- **PR 本文の承認 commit が `040e608f` のまま残っていた。** `reviewed.json` は `1bcdbf54` へ更新済みだった。
- 同じ「検査と確定操作を同一連結に入れる」誤りをこのサイクルで3回踏んだ（`reviewed.json`・教訓の昇格・ADR-0039 の追記）。1回目は後続の検査が古い値を印字して気づき、3回目は実行前に grep で未反映を確認した。2回目だけが自己申告のまま通った。

## 記録

- 承認 commit: `b2ed24504d5ef33327571323e3b185850f716866`（第3ラウンド後）。第2ラウンド後は `1bcdbf54` だった。
  第2ラウンドの probe が読んだのは rebase 前の `040e608f` で、base が PR-A と #1210 を取り込んだ後に rebase した結果この SHA になった。
  両者の `plugin/pfdsl` と `plugin/pfdsl-codex` は `git diff` で差分ゼロなので、probe が読んだ配布ツリーとこの commit の配布ツリーは同一である。
  rebase で取り込んだ差分に配布物は含まれない（`.pfdsl/workflow.md` の hook 一覧から `stale-dist-guard` が1件消えたのが唯一の規約文書の変更）。
- 対象ファイル数: 第1ラウンド 24（`plugin/pfdsl` 12・`plugin/pfdsl-codex` 12）、第2ラウンドは第1ラウンドの承認 commit 以降の差分
