# 配布レビュー実行記録 2026-08-22（差分モード）

- base: `e0deb8c3a73881c938a97c13a901cd30eddbf93d`
- reviewed commit: `ad097dad8ff4ef3143c346c00c9a73aab4bb00b8`
- 公開 CLI: npm の `@pfdsl/cli@0.0.25`
- 対象: 9ファイル

対象ファイルは次のとおり。

- `pfd-cycle/SKILL.md`
- `pfd-init/SKILL.md`
- `pfd-ops/SKILL.md`
- `pfd-ops/references/architecture.md`
- `pfd-ops/references/github-issues-backend.md`
- `pfd-ops/references/work-cycle.md`
- `pfdsl/SKILL.md`
- `pfdsl/references/quality-guide.md`
- `source-command-pfd-retro/SKILL.md`

各 valid probe は専用 sandbox と専用の `plugin-cache/pfdsl` コピーで実行した。
各 probe に同じ sandbox は共有していない。

## 課題と valid probe

| probe | 課題 | 結果 |
|---|---|---|
| command discovery/cycle | 配布された command の発見から cycle の起動と retro の継続条件までを確認する | 詰まりなし |
| pfd-ops ready/first safe step | ready な実装候補を選び、最初の安全な手順を特定する | 詰まりなし |
| pfdsl quality check | 公開 CLI と同梱ガイドを使い、品質チェックを実行する | W002 の criteria 品質問題を検出 |

3本の valid probe はすべて上流境界を越えなかった。
Claude Code や `.claude` への依存による滞留もなかった。

command probe で観測した command 名は `pfd-init`、`pfd-cycle`、`source-command-pfd-retro` だった。
利用者向けの retro 起動名は `/pfd-retro` だった。

pfd-ops probe は `implement` が ready/best であることを確認し、companion が branch・worktree・setup の手順を供給した。

pfdsl probe は公開 CLI と同梱の quality guide を使い、期待どおり W002 の criteria 品質問題を検出した。

valid probe は上流 worktree、`plugin/pfdsl/...`、global `.Codex/skills` を読んでいない。
読んだのは sandbox の repo と `plugin-cache/pfdsl` のパスだけだった。

## 初回 invalid probe

初回の command probe は、環境にある global な `git fetch origin` 規則で停止した。
これは環境制約であり、結果は invalid として破棄した。

初回の command/ops fixture は、roadmap を repo root に置くか companion を省略していた。
これは課題設計の不備であり、結果は invalid として破棄した。

2件の invalid probe は、いずれも fresh sandbox で課題と fixture を組み直して rerun した。

## とりまとめ

| 分類 | 件数 | 対応 |
|---|---:|---|
| (a) 配布プロンプトの欠陥 | 0 | bundle 修正なし |
| (b) 課題設計の不備 | 2 invalid probe | fresh sandbox で組み直して rerun |
| (c) 環境制約 | 1 invalid probe | 記録のみ |

bundle の修正は行っていない。
一般化できる lessons の昇格も行っていない。

差分モードのため、承認済み commit を `ad097dad8ff4ef3143c346c00c9a73aab4bb00b8` に進める。
