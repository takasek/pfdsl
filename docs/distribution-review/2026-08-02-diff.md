# 配布レビュー実行記録 2026-08-02（差分モード・初回）

- base: 空ツリー（`reviewed.json` の `commit` が `null`。初回なので差分＝配布ツリー全体）
- 対象: スコープ内22ファイル
- probe: 3本、各1 sandbox

`docs/distribution-review.md` の観点は probe には渡していない。
渡したのは課題だけで、観点は詰まりの分類にこちら側が使う。

## 課題

| probe | 課題 | 通る配布物 |
|---|---|---|
| retro | companion に溜まった汎用ルールらしき記述を見つけ、昇格先を決めて実行する | pfd-retro SKILL.md、pfd-ops architecture.md・work-cycle.md、scaffold bindings |
| ops | ワークサイクルの前提を整え、作業項目を1周させ、得た知見の置き場を決める | pfd-ops SKILL.md・work-cycle.md、scaffold 一式 |
| ecosystem | テンプレのまま置かれた `.pfdsl/` を、必要な種別の判断からプロジェクト実体に合わせるまで | pfd-ecosystem SKILL.md・kind-taxonomy.md、pfd-init コマンド |

## probe: retro

越境なし。
報告中のパス参照はすべて sandbox 内で、上流リポ名への言及は bundle 自身の `architecture.md` を読んだ結果だった。

### (a) 配布プロンプトの欠陥

1. **`pfd-ops/references/work-cycle.md:49` が上流リポを主語にしている。**
   「**バックストップ**（(a) について、**このリポでは**）: … `scripts/lib/retro-reminder-check.mjs`」。
   利用側は「このリポ」を自分のリポと読み、存在しないパスを指されて止まる。
   bundle は同じ役割の `hooks/retro-reminder-post-tool-use.mjs` を同梱しているのに、この文はそれに触れていない。
   機械検査の2ルールはどちらも踏まないため素通りしていた（観点5「非パス形の上流前提」と観点2「実施先の違う行為」の複合）。

2. **`pfd-retro/SKILL.md:70` の起票が、issue バックエンドを持たない採用リポで実行不能になる。**
   「上流リポへの変更提案として起票する — 検出結果を捨てない」に対し、バックエンド未採用のリポには起票先が無い（probe の環境では remote も `gh` も無かった）。
   出力表の「未着手作業の発見 | issue 起票 + roadmap.pfdsl に依存チェーン追加」も同じ穴を持つ。

3. **`pfd-retro/SKILL.md:64` の宛先上書き経路が未記入プレースホルダで行き止まる。**
   「宛先の上書き・追加は pfd-ops の workflow companion に従う」→ scaffold の companion は「(経路を記載する。)」のまま。
   未記入だったときに基本表へ戻れという指示が無い。

4. **pfd-retro に前提条件が無い。**
   pfd-ops は `SKILL.md:20` で `.pfdsl/` 不在なら `/pfd-init` へ案内して終了するが、pfd-retro は scaffold のままの図に対して A〜D 監査を最後まで走ってしまう。

5. **`.pfdsl/bindings/pfd-retro.md` の scaffold に、書けと言われた内容の置き場が無い。**
   `SKILL.md:62` は「問いの構造・パターン種別・具体例をここに書く」と指示するが、scaffold は見出しの無い7行のプレースホルダで、probe は自分で見出しを発明した。
   別のリポで回せば別の見出しになる。

6. **「A〜D の監査」とカタログの「A/B/C」で文字が衝突している。**
   スキルの節は A・B / C（運用イベント）/ D（知識成果物）、カタログは A/B/C（C は仕様・制約）。
   `SKILL.md:26` が「カタログの C 系」と書いて部分的に回避しているが、コマンド本文の「A〜D」と並ぶと読み手が突き合わせを要する。

### (a) だが判断を要するため今回は記録に留めたもの

- **L4 滞留監査に scaffold 除外が無い。** 「companion（L4）は配布されないため」という根拠は、採用直後の companion が scaffold そのままである間は成り立たない（scaffold は配布されている）。検出器の判別軸が「固有名詞を含まない」という構文だけなので、未記入の採用リポでは候補が全件偽陽性になる。
- **固有名詞の列挙が2箇所で違う。** `pfd-retro/SKILL.md:47` は「リポ名・パッケージ名・ツール名・パス」、`architecture.md:52` は「リポ名・パッケージ名・ツール名・パス・issue 番号・ADR 番号」。同じ概念の列挙が2箇所にあり、スキル自身が禁じている列挙ドリフトの形。
- **companion に書く言語の規約が無い。** scaffold は日本語、採用リポは英語のことがある。

### (b) 課題設計の不備

- 課題文が「project-specific proper nouns を含まない規則」と書いたが、スキルの述語は「固有名詞」で、パスやツール名も含む。probe はこの差で矛盾を疑い、判断に時間を使った。**課題はスキルの述語をそのまま使うこと。**
- 課題が「汎用ルールが1つある」と前提を与えたが、sandbox の companion は全行が scaffold のままなので前提が成り立たなかった。**課題の前提が sandbox の初期状態と整合しているか、投入前に確認すること。**

### (c) 環境制約

- 単一コミットのため `git diff <range>` 系の監査手順が実行不能
- 過去セッションが無いため「保留した違和感の想起」が実行不能
- `gh` 不在（ただし上記 (a)2 の穴は環境制約ではなく、バックエンド未採用という正当な採用形態で起きる）

## probe: ecosystem

越境なし。
参照はすべて sandbox 内で、上流リポの実体には触れていない。

### (a) 配布プロンプトの欠陥（機械で再現確認済み）

1. **scaffold の `roadmap.pfdsl` が配布状態で `fmt --check` を落とす。**
   `waiting:` 行の桁揃えが正準形でない。
   採用リポはこれをコピーした時点で未整形のファイルを持つことになる。
   `make check-fmt` は `.pfdsl/` しか見ないため、配布物である scaffold はこの検査の外にある。

2. **`pfd-ecosystem/SKILL.md` の scaffold ファイル一覧に `review-perspectives.md` が無い。**
   一覧は8ファイルだが、scaffold には9ファイル目として `review-perspectives.md` が実在する。
   一方 scaffold の `bindings/pfd-retro.md:3` は「当リポで検出した具体例は `.pfdsl/review-perspectives.md` に蓄積する」と指す。
   手順どおりコピーすると、この参照先が生成されないまま残る。

3. **plugin manifest の description がコマンド構成とずれている。**
   `/pfd-retro` はコマンドとして配布されているのに `plugin.json` の description に無い。
   `marketplace.json` 側はさらに古く、pfd-grill・pfd-ops も落ちている。

4. **`graph io` の終端列挙と `externalStakeholders` の推奨が互いを打ち消す。**
   `pfd-ops/SKILL.md:67` は「終端 artifact 一覧は `graph io <file> --json` で機械列挙できる」と述べ、`:68` は「外部消費者は `externalStakeholders` フィールドで明示」と勧める。
   後者に従うと当該 artifact が `terminals` から外れる（最小例で再現確認）。
   結果、終端ゲートの監査対象が空になり、監査したつもりで何も見ていない状態になる。

5. **`status gaps` の説明が保証を過大に述べている。**
   `pfdsl/SKILL.md` は roadmap と flow の整合点検として案内するが、`kind-taxonomy.md` は flow ファイルに通常 status を書かないとしている。
   正しく型付けされた flow は todo artifact を持たないので、この検査は構造上つねに緑になる。

6. **workflow 非採用のリポで findings の宛先が消える。**
   `pfd-ops/SKILL.md:53` と scaffold の `bindings/pfd-retro.md:7` はどちらも `.pfdsl/workflow.md` を条件なしで名指しする。
   roadmap 非採用は `pfd-ecosystem/SKILL.md:94` が N/A と明示するのに、workflow 非採用には対応する記述が無い。

7. **`pfd-ops/SKILL.md:20` の前提条件が存在チェックだけになっている。**
   scaffold をコピーしただけのリポは条件を満たしたことになり、全ノードがプレースホルダのままの図に対して運用プロトコルが走る。
   probe: retro の指摘4と同じ穴を逆側から踏んだもの。

8. **`pfd-ecosystem/SKILL.md:94` と `pfd-ops/SKILL.md:62` が衝突する。**
   前者は作成した `.pfdsl` を roadmap に artifact として登録せよと言い、後者は他作業をゲートしない bookkeeping を roadmap に載せるなと言う。
   どちらが優先するかが書かれていない。

9. **scaffold の `roadmap.pfdsl` が issue トラッカーの存在を無条件に前提している。**
   `pfd-ecosystem/SKILL.md:59` はバックエンドを「（任意）」としているのに、scaffold は「issue 本体が一次情報、本ファイルは依存構造のみ管理」と書いた状態で配布される。
   トラッカーを持たないリポでは前提が最初から偽になる。

10. **`check` の strict 指定が2箇所で食い違う。**
    `pfd-ecosystem/SKILL.md:93` は `pfdsl check <file>`、`pfd-grill/SKILL.md:54` は `--strict` 付き。

### (c) 環境制約

- `${CLAUDE_PLUGIN_ROOT}` は probe のシェルに export されないため、配布物中の bash ブロックをそのまま実行すると失敗する。plugin ホスト下では置換されるので probe 固有の制約。ただし「変数名のまま見えていたら repo-local」という判別規則は、プロンプト上の見え方とシェルの展開結果を同一視しており、文言としては手当てが要る
- 種別の問診・剪定・`criteria` の確定はいずれもユーザー対話を前提とする手順で、probe には相手がいない。probe は自分で答えを埋めたと明示的に報告している（設計どおりの制約）

### (b) 課題設計の不備

- 課題が「partly-filled」と書いたが、sandbox は全ファイルが scaffold そのままだった。probe は diff で気付いて対処したが、課題文と実体が食い違っていた

## probe: ops

越境なし。

### (a) 配布プロンプトの欠陥

1. **`work-cycle.md:49`（probe: retro と独立に再検出）。**
   2本の probe が別の課題から同じ行に到達した。

2. **`work-cycle.md:46` が配布されていないコマンドを終端ゲートに書いている。**
   「`/simplify` または `/code-review` を実施した」。
   どちらも plugin に同梱されていないため、採用リポにはチェック項目を満たす手段が無い。
   観点5「非パス形の上流前提」そのもの。

3. **`work-cycle.md:35`・`:40` が同梱されていないゲート集約チェッカーを前提にしている。**
   `:40` はチェッカーの出力がゲートの入力であるかのように書かれているため、チェッカーが無いと手順が定義されない。

4. **`scaffold/roadmap.md` にファイルベース・トラッカーのプリセットが無い。**
   `pfd-ecosystem/SKILL.md:59` はバックエンドを任意としているのに、同梱プリセットは GitHub Issues だけ。
   markdown ファイルで管理するリポは、ID 規約・flow 分類・status 同期・完了規則を丸ごと自作することになる。

5. **`work-cycle.md:44` のコミット粒度規約に置き場が無い。**
   「規約（CLAUDE.md または companion で定義）に従っている」と書くが、scaffold の companion にはその節が無い。

6. **`pfd-cycle.md:5` は「1〜4」と言うが `work-cycle.md` は5手順を定義している。**
   retro が5番目で、コマンド側は retro を別途名指ししているため偶然だけ整合している。

7. **`externalStakeholders` と `graph io` の相殺（probe: ecosystem と独立に再検出）。**
   `work-cycle.md:40` の終端ゲートも `graph io` に依存しているため、影響範囲は pfd-ops SKILL.md だけではない。

### (c) 環境制約

- remote が無いため `git fetch origin` と `origin/<base>` からの分岐が実行不能。PR を前提とする終端ゲート項目も同様
- protocol 7(b) のユーザー確認は相手がいないため実行不能（設計どおり）

## 3本を通した収束

複数の probe が独立に同じ場所へ到達したものは、単発の指摘より優先度が高い。

| 件 | 到達した probe |
|---|---|
| `work-cycle.md:49` の「このリポでは」＋非同梱スクリプトパス | retro, ops |
| `externalStakeholders` が `graph io` の終端列挙を空にする | ecosystem, ops |
| companion に書く言語の規約が無い | retro, ecosystem, ops |
| scaffold のままの `.pfdsl/` を「採用済み」と誤判定する前提条件 | retro, ecosystem, ops |
| issue バックエンドを持たないリポで findings・起票の宛先が消える | retro, ecosystem, ops |
