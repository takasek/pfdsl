# 配布レビュー実行記録 2026-08-12（差分モード・3回目）

- base: `f41fe18`（2026-08-04 の承認 commit）
- 対象: スコープ内12ファイル（#905 までの束マージ分 + 本サイクル #912 の編集4ファイル）
- probe: 3本、各1 sandbox
- 前提: 本サイクル #912 は誤誘導文の修正と bundle 内同文再掲の統合。probe は統合後のポインタ連鎖を実走させる形で組んだ

`docs/distribution-review.md` の観点は probe には渡していない。
渡したのは課題だけで、観点は詰まりの分類にこちら側が使う。

## 課題

| probe | 課題 | 通る配布物 |
|---|---|---|
| lens | pfd-lens agent 定義に従い、播種済みの欠陥（配線忘れ・criteria 無し終端）を含む roadmap を監査する | pfd-lens.md → pfd-retro SKILL.md（統合後のポインタ連鎖）→ pfdsl references/review-perspectives.md |
| init | pfd-ecosystem ステップ1〜3で `.pfdsl/` を初期構築する | pfd-ecosystem SKILL.md（統合後のポインタ）→ pfd-ops SKILL.md 変数解決規則、scaffold 一式（新 binding 書式・sample-pattern 含む） |
| stake | 最終納品物に `externalStakeholders` を宣言し、書いた後の点検を行う | pfdsl SKILL.md（CLI プリフライト・「読解と点検」= 統合の一次情報側） |

CLI は npm 公開版 `@pfdsl/cli@0.0.25` を各 sandbox に導入した。

## probe: lens

越境なし。
統合後の連鎖（agent 定義 → plugin 側 pfd-retro SKILL.md → binding 不在 → plugin 側 review-perspectives.md）を指示どおり踏破し、播種した2欠陥を両方検出した。
詰まり1件: agent 定義の手順5が指す `graph describe` が公開版 0.0.25 に無い（`unknown graph subcommand: describe`）。
probe は「読み取り専用名前空間」の記述を根拠に `neighbors` / `stats` で代替し監査を完遂した。

分類: (c) 環境制約。
`describe` への言及は base 以降に足された記述で、marketplace の plugin 配布は v0.0.25 タグに pin されているため、この記述が採用リポへ届くのは pin を進める次リリース（= `describe` を含む CLI の公開）と同時になる。
リリース前の作業ツリーと公開版の版ずれであり、出荷境界で整合する。
配布物は直さない。

## probe: init

越境なし。詰まりなし。
統合で pfd-ecosystem 側から削った変数解決の3分岐を、新設のポインタ経由で pfd-ops SKILL.md「配置ファイルの鮮度セルフチェック」から正しく引き、3つ目の分岐（自ファイル所在から相対で辿る）を適用して scaffold 6ファイル（roadmap 種別分 + 種別非依存分）を正しく選別コピーした。

## probe: stake

越境なし。詰まりなし。
統合の一次情報側（pfdsl SKILL.md「読解と点検」）だけで `externalStakeholders` 宣言 → `terminal artifacts:` 空化 → `externalTerminals` への遷移を逐語どおり解釈・報告した。
公開版 0.0.25 でもこの挙動は実装済み。

## 机上照合（probe を組まなかった差分）

- `gh issue view` の取得形（work-cycle.md 手順1、本サイクルの修正）: 修正前の形（`--comments` 単独）が本文を落とすことは gh の実挙動として本サイクルの作業中に直接確認済み。修正後の `--json body,comments` は同確認で有効
- pfdsl SKILL.md の CLI 列挙に公開版 0.0.25 に無いサブコマンド（`locate` / `describe` / `meta list` / `values`）が含まれる件: lens probe の詰まりと同じ (c)。プリフライトの版下限は `{{cliVersion}}` テンプレート変数で CLI 版に自動追随するため、値の陳腐化は構造的に起きない（2026-08-04 の教訓「数値は追随しても理由は追随しない」の値側は機構で担保済み）
- pfd-retro SKILL.md の D 層追記（持ち越し状態・停止しない機構）・quality-guide の2行・review-perspectives の到達性観点・github-issues-backend の `no-issue:` 宣言・scaffold の新 binding 書式: いずれも観点1〜7で通読し、上流固有の固有名詞・実施先の違う行為・裸の参照は検出なし。scaffold binding の閾値根拠は「上流（本 binding を配布するリポ）の実測1点」と読み手側の軸で明示されており観点3を満たす

## とりまとめ

(a) 配布プロンプトの欠陥: 0件。
(b) 課題設計の不備: 0件。
(c) 環境制約: 1件（`graph describe` の版ずれ。出荷境界で整合するため対応不要）。

修正0件のため再走なし。
hash は本記録と同時に進める（差分モード）。
