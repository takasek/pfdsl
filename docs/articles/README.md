# PFDSL 連載記事

PFDSL の問題意識と設計根拠を説明する公開技術記事（連載・日本語）。issue
[#12](https://github.com/takasek/pfdsl/issues/12) に対応し、リポの生態系図上は `write_article` の出力 `i12_article`。
この README は連載の**構成プラン（設計書）**。本文はこの後 `docs/articles/NN-slug.md` に書く。

**書き方の基本方針（R5 で確定、R8 で構成を痛み階段へ再設計、R9 で第1部+第2部を統合し3部構成へ、R10 で体験談枠取りを全廃し仮想事例へ）**: 序・hook で釣らない。伏線/回収/「○○へ橋渡し」式の物語的レトリックを使わない。
**素直な技術記事として「痛み（問題）→ 解決」で書く**。免責・N=1 開示・反実仮想の限界・自己批判の連打は誠実さでなく言い訳/ノイズとして削ぐ（残すのは各部の負例1件のみ）。
**R10（2026-07-06 ユーザーレビュー）**: 読者は事例が実話かどうかに興味がない。「著者が」「実話」「自己観察」の枠取り・N=1 の一文は全廃し、payoff log は素材に留めて「実際に起こりうる分かりやすい仮想事例」として再構成する（「〜としよう」導入可）。抽象論とポエミーな表現を排し、平易かつ簡潔に書く。文章規範は japanese-tech-writing スキル（`~/.claude/skills/japanese-tech-writing`）に全面的に従う。
読者の現場への適用手順（「あなたのリポジトリで」）は**別記事**で本連載のスコープ外。

## 解く問題（記事全体の頂点・一文）

> **プロジェクトの進め方 — 誰が何を作り、何に依存して完了するか — が暗黙知のままだと、人も AI も毎回文脈の口移しでしか参加できない。
> プロセスを成果物依存グラフとしてテキストに外化し機械検査に載せれば、文脈ゼロの参加者が図を読むだけで作業を選び、回し、監査できる。**
> — PFDSL はそのための言語と運用。

逆ピラミッド: この頂点（＝第3部の到達点の約束）を第1部の冒頭近くで言い切る。
以降の各部はこの到達点に必要な性質を痛み駆動で1つずつ実証する。各部末で要点を再宣言しない。

## 読者

プロセスをコードと同じく**テキスト・版管理・CI に載せたい開発者**（git/diff/レビュー/CI を回し、AI エージェントにも仕事を振り始めた層）。
PFD の前提知識は仮定しない。第1部の入口だけ「依存関係は誰の仕事にもある」と広く入り、以降は開発者の既知概念（git・diff・CI・レビュー・subagent）で内部用語を橋渡しする。

## 連載の構成 — 痛み階段（R8、R9 で3部に統合）

3部 = 読者の3つの痛み。**各部は「痛み → 解 → 実演・事例 → 払うコスト（負例1件）」の完結構造**。
事例の素材は `docs/pfd_payoff_log.md` から各部に配分する（下のエピソード配分表が正）。R10 以降、本文では実話性を主張せず仮想事例として再構成する（配分表・節構成の「実話」は「事例素材」と読み替える）。旧構成（言語設計を前半・実践を後半）は実話が後半に偏り前半が内向きの設計解説になったため廃止。
R9: 旧第1部（着手可能集合）と旧第2部（図は腐る）を統合して新第1部とした。
旧第1部単体は「具体例はあるが情報として分かり切った話」に留まり、負例（前倒しモデリングの初期コスト）もコストとして弱いとレビューで却下されたため。
統合後の第1部は「既存のタスク管理はなぜ依存関係を表現できないか」を最初の焦点に据える（比較の従属化を撤回し主題化する）。

| 部 | 痛み | 解（実演する性質） |
|---|---|---|
| 第1部 | ToDo・WBS 等の既存タスク管理では依存が表現できず、作図しても図が腐る | 成果物依存グラフ + status → 着手可能集合の機械導出。テキスト性 → diff・レビュー・CI に載る |
| 第2部 | 構造の見落としは人が拾えない | PFD 意味論の機械検査 + 検査可能性を守る設計判断 |
| 第3部 | AI・新規参加者に文脈を口移しする | 運用自体を PFD に焼き付け、汎用スキルが読んで回す |

3条件（生成依存の明示・機械検査・テキスト性）は**各部では名指しせず**、第3部末で初めて命名して総括する（下記第3部§5）。

## 第1部 節構成 — 「今、何に着手できるか」（R9: 旧第1部+旧第2部を統合）

冒頭に頂点の一文を言い切る（連載の道筋段落は数文以内・目次代わり・3部構成）。

1. **痛み — 既存のタスク管理は依存関係を表現できない**（最初の焦点はここ。「依存は誰の仕事にもある」の広い入口は1〜2段落に圧縮する）。
   ToDo リストはフラットで、項目間の「何が済めば何ができるか」を持てない。
   より高級な手法も同様: 依存管理の代表 WBS は deliverable 指向だが包含ツリーで*依存グラフではない*（「AはBの一部」は書けても「AからBを作る」の生成エッジを持たない）。
   DFD はデータの流れを書くが状態更新・データストアを持ち、作業の完了・着手可否を扱わない。
   作図ツールで依存の絵は描けるが更新が続かず、実体と乖離して死蔵される（乖離した図は参照されなくなり、参照されない図は更新されない）。
   **なぜ難しいかの分析を語りの軸にする**: どの手法も「成果物」と「何から何を作るか（生成依存）」を第一級の要素として持たないから。
2. **成果物で考える** — プロセスでなく成果物を明示すると「何を生んで完了したか」が検証可能になる — これが清水吉男の PFD の核（PFD をここで1文定義）。
   README の最小例で最小記法（`A >> P -> B`・集合記法・status frontmatter）を導入する。
3. **着手可能集合の機械導出** — 入力が全て done のプロセス = 今着手できる作業。`ready` 相当の導出を動作で見せる。
   事例2件（素材: payoff log 2026-06-11×2）: オープン issue 8件を図に落とすと並列着手9本が status から即答できる／新提案の受け入れは既存グラフへの1チェーン追加で並列性・接点・合流点が確定する。
   （N=1 の一文は R10 で廃止）
4. **テキストだから git に載る** — 座標を持たずグラフ構造だけを書く。変更が diff になり、diff がレビュー対象になり、履歴が残る。
   これが §1 の「図は腐る」への答え: 実体との同期がコードと同じ営み（レビュー・CI）に載る。
   `check` を動作で見せる: 「絵を出すだけなら Graphviz でできる。PFDSL が足したのは絵でなく、PFD 固有の制約を構文として検査する層だ」を、Graphviz では出ないエラーが PFDSL では出る**対比デモ**として実演内に示す。
   CI に組み込めば図の腐敗が検出可能になる。先行例（SWET の PFD Draw Tool）・diagram-as-code は各1文で従属的に触れる。
5. **払うコスト（負例1件）** — テキスト化してもグラフと作業実体（issue 等）のドリフトは残り、同期を保つ監査ステップが要る。
   テキスト化はドリフトを消すのでなく、検出可能にする。
   （旧第1部の負例「前倒しモデリングの初期コスト」は R9 で廃止 — コストとして弱いとレビューで却下）

## 第2部 節構成 — 「構造の見落としは人が拾えない」（旧第3部）

1. **痛み** — ガイドとレビューを整備してもミスは再発する。
   実話: 品質ガイドの例文に明記済みの入力欠落が、その直後の実践ラウンドで再発した。消費されない成果物の放置も2例。
   列挙的・網羅的な確認は人の認知に向かない — ルールの質の問題ではない。
2. **PFD 固有の意味論を機械検査する** — 単一生成元・DAG 性・終端健全性・孤立プロセス・完了基準の欠落を検査する（テキストで CI に載ること一般でなく「何を検査するか」が PFD 固有）。
   ルールと機械検査の**二層分業**をここで1段落: ルールは設計判断（粒度・命名・外化の要否）だけを担当し、列挙的確認は全部機械に送る。
   実話3件: 作った警告が自分の roadmap の完了基準欠落を直撃した／運用で見つけた見落とし様式（チェーン削除後の孤立プロセス）を lint 要件に変換し、以後は機械検出になった（閉ループ）／文脈を持たない外部レビュアー（AI）にリポ構造を監査させたら、指摘14件が全て機械検査の届かない散文層に集中し、機械化済みの領域からは新規欠陥が出なかった — 分業の防波堤が指摘の分布として可視化され、次に機械化すべき位置まで特定された。
3. **検査可能性を守るために入れない機能** — 統一理由は「文脈ゼロの読者（人も機械も）が静的に読み切れる図であることを壊すから」。
   update（スナップショット + ビューで代替。**改版3形態と `>>?` をここで図つき導入** — 別 artifact／`>>?` 収束／定常サイクル）／条件分岐（合否・承認は承認記録・結果報告書 + `>>?` へ変換。清水 PFD も元々持たない）／subroutine（似た Process は横断ラベルで代替）。
4. **図そのものが検査装置になる** — 機械検査に載る手前で、図に書くこと自体が構造を暴く。
   実話3件: 「並列で起草できる」という直感が図で反証され、相互依存の発見につながった／仕様の境界ケースを図の上でトレースした検証ラウンドがそのまま11のテストケースになった（仕様テキストだけでは非自明ケースを事前網羅できなかった）／相互依存を1プロセス複数出力に畳んだ roadmap が、後続着手者が罠を踏む前に正解構造を渡した。
5. **払うコスト（負例1件）** — 粒度の読み違えは残る。
   粒度ルールに従って一度は統合したプロセスが、委譲した一括起草が存在しない参照 ID を捏造して初めて「隠れた独立プロセスだった」と分かり、分割し直した。
   図が事前に正解を出したのでなく、失敗が入力不足を露出させた。

## 第3部 節構成 — 「AI に文脈を口移しする」（旧第4部）

1. **痛み** — エージェントにも新規参加者にも、毎回プロジェクトの文脈を口移しで説明する。
   委譲した subagent は暗黙知を踏み抜く。進め方が人の頭の中にしかない限り、参加コストは人数と回数に比例する。
2. **運用を PFD に焼き付ける** — プロジェクトの運用そのものを外化する: **roadmap**（何を何の前に作るか・今何に着手できるか。status を持つ）と **workflow**（この作業はどう繰り返されるか・誰が何をトリガーに何を行うか）。
   それを**リポ非依存の汎用スキル**が読んで回す。層設計は平易語で圧縮: 汎用手順は PFD 概念だけから導ける（着手判断＝入力が全 done・進捗＝status 更新・1サイクル1プロセス）／リポ固有の中身は図の隣の説明ファイルに逃がす／issue 管理の流儀は選んで載せるプリセット。
   スキル名（pfd-cycle / pfd-ops / pfd-retro）はここで一度だけ紹介し、以降「運用ハーネス」で受ける。
3. **実証** — （R10: 射程の一句は廃止。仮想事例として書く）。
   文脈ゼロの AI エージェントが roadmap/workflow を読んで着手可能集合を導出し、1サイクル完走。その過程で設計者の見落とし2件（issue クローズの timing・チェーン未定義による孤立プロセス）を露出させ、直して再実行したら消えた。
   外部リポへの初適用でリポ非依存を確認: スコープ変更2件を完了基準の1行編集とチェーン1本の追記で吸収し、再構成が不要だった。
4. **払うコスト（負例1件）** — 焼き付けてもルール文言に書かれない意図（暗黙スコープ）は残る。
   実話: 運用ルールの「open のもの」という文言が意図した適用対象を持たず、誤適用が起きた。定期監査が「あるべき記述の不在」としてこれを拾い、文言が修正された。
5. **総括 — 3条件の命名（連載で唯一ここ）** — ①成果物と生成依存エッジの明示（第1部§2〜3が実演）②PFD 意味論の機械検査（第2部）③テキスト性（第1部§4）。
   三つが揃うと、文脈を持たない参加者が図を読むだけで作業を選び、回し、監査できる — 頂点の一文の実証が完了する。
   **「回収」「伏線」等のメタ語は使わない**。命名と対応の提示だけで締める。

## 書き方の規律

- **痛み（問題）→ 解決**。各部・各節は「次に解く痛み・小問題」から淡々と始める。前部を引き継ぐ宣言（「ここまでが…だが」）や次部への予告（「第N部で詳述」）を書かない。
- **物語的メタ装置を使わない**: 「橋渡し」「コーダ」「蝶番」「結」「回収する」「連載を閉じる」式の構造語を本文に持ち込まない。否定形での自己言及も書かない。
  第3部§5の3条件命名は総括であって回収レトリックではない — 命名と対応提示のみで書く。
- **免責は各部の負例1件のみ**（それ以外の自己批判・方法論的懺悔は本文に書かない）:
  各部に「払うコスト」型の負例を**1件だけ**（第1部: ドリフト同期監査／第2部: 粒度の読み違え／第3部: 暗黙スコープ）。読者が同じ道具で実際に踏む落とし穴に限る。
- **見出し**: H1 は「タイトル（PFDSL 連載 第N部）」の括弧形式。地の文・見出しにダッシュ（—）を使わない・並列の中黒を使わない等、整形は japanese-tech-writing に従う。
- **記号・略号を本文に出さない**: `ADR-NNNN`/`V001`/`W002`/`V020`/`L1〜L4`/`cold subagent`/`companion` 等は平易語に開き、出典は脚注へ。
- **dogfooding という語を使わない**: 「著者が自リポジトリで」等の平易語で受ける（未定義語を増やさない）。
- **表は最小**: 短い対比（3項目以上・各セル1行）だけ表。物語・事件・反実仮想は散文。本 README の整理用の表は本文に出さない。
- **用語の初出マップ**: PFD の1文定義・最小記法・status・着手可能集合＝第1部§2〜3／`check`（動作として）・diff・CI＝第1部§4／単一生成元・DAG・終端健全性＝第2部§2／`>>?`・改版3形態＝第2部§3（図つき）／roadmap・workflow（種別）・運用ハーネス＝第3部§2／3条件の命名＝第3部§5のみ。

## エピソード配分（payoff log → 部）

本文執筆時はこの表を正とする。出典は `docs/pfd_payoff_log.md` の日付見出し。

| 実話 | 部・節 |
|---|---|
| 2026-06-11 着手可能集合の機械導出（9本並列即答） | 第1部§3 |
| 2026-06-11 新提案の受け入れコスト低減（1チェーン追加） | 第1部§3 |
| 2026-06-19 W002 拡張が自 roadmap の設計不備を即時検出 | 第2部§2 |
| 2026-06-19 lint 要件経路の完結（孤立プロセス→V020） | 第2部§2 |
| 2026-07-06 機械検査の防波堤が構造レビューの findings 分布で実証された | 第2部§2 |
| 2026-06-11 暗黙依存のレビュー可能化（並列→収束） | 第2部§4 |
| 2026-06-21 ADR の具体例がそのまま TDD fixture になる（11テスト） | 第2部§4 |
| 2026-06-20 相互依存の先回りモデル化が並列主張の罠を未然回避 | 第2部§4 |
| 2026-06-12 粒度判断は観測で更新される（統合→委譲失敗→再分割） | 第2部§5（負例） |
| 2026-06-14 dogfood ループによる設計盲点の検出と閉ループ修正 | 第3部§3 |
| 2026-07-03 pfdsl 開発リポ外への初適用 | 第3部§3 |
| 2026-06-19 retro C層が運用ルールの暗黙スコープを検出 | 第3部§4（負例） |
| 2026-06-16 忘れ物・列挙ドリフトの機械的検出 | 第1部§5（負例の裏付け・軽く） |

**不使用**: code-review 2件（2026-06-15×2。PFD ハーネスの dogfood でない）／仕様統合外部レビュー（2026-06-15。言語開発の内部話）／形骸化検出・効果測定装置（2026-06-11×2。第2部§1〜2 の背景素材としてのみ・独立エピソード化しない）。

## 言語

日本語。CLAUDE.md「公開ドキュメントは英語」への意図的例外（対象が日本の方法論＝清水吉男の PFD、読者・媒体が日本語）。
ファイル名・slug は英語。

## 配置・命名

各部の本文 — `docs/articles/NN-slug.md`（序は作らない）。
slug: `01-what-can-we-start-now` / `02-machine-checked-structure` / `03-zero-context-participants`（R9 で旧 `02-diagrams-rot` は第1部に統合され消滅）。

## 公開先

Zenn で確定（2026-07-03）。
本文執筆完了後、zenn-cli のリポジトリ管理（`articles/` 配下・Zenn slug 命名・frontmatter 付与）へ移行する。
移行までは `docs/articles/NN-slug.md` で執筆・レビューを回す。

## 素材

- `docs/pfd_payoff_log.md`（実話の一次情報。上のエピソード配分表で引く。成功例のみ＝生存者バイアスに注意。負例は git 履歴・ADR から採る）
- `docs/adr/`（0001〜0006 が言語設計の核。負例出典: 0009・0019・0021）
- `README.md`、`docs/spec/spec.md`
- 負例の一次資料: 粒度ルールに従った統合→失敗→再分割（ADR-0009 の4段階変遷）、運用ルールの暗黙スコープ（roadmap.md 修正 commit）
- 背景: SWET ブログ2本（本文で軽く一度・比較しない） https://swet.dena.com/entry/2024/11/15/120000 / https://swet.dena.com/entry/2026/01/16/120000

### 関連トピック

R9 で WBS・DFD は第1部§1の主題側（「なぜ既存手法では依存が表現できないか」の分析対象）に昇格。
他（diagram-as-code・SWET 先行例等）は従来どおり本文で軽く・主題化しない。

- **WBS**: PMBOK 定義は deliverable-oriented だが包含ツリーで*依存グラフでない*（生成エッジを持たない）のが PFD との差分。
  https://www.pmi.org/learning/library/practice-standard-work-breakdown-structures-8063 / https://en.wikipedia.org/wiki/Work_breakdown_structure
- **DFD**: 状態更新・データストアを持ち循環しうる／PFDSL は純粋関数 DAG で update を採らない（ADR-0003）。
- **diagram-as-code**: Structurizr 等がテキスト＋AI パース＋drift 検出を同方向で主張。https://structurizr.com/
- **清水吉男 XDDP / PFD**: 『「派生開発」を成功させるプロセス改善の技術と極意』、AFFORDD https://affordd.jp/derivative-development/xddp/method/
- **（任意・脚注程度）プロセス外化と AI**: Externalization in LLM Agents（arXiv:2604.08224, 2026, 実在確認済み） https://arxiv.org/abs/2604.08224 — 論拠にはせず、近い現象を自リポで1例観測したと記す程度。
  harness engineering の語は無理に使わない。

## 執筆者メモ（本文に出さない・self-check 用）

provenance 事故・altitude 混同を防ぐための控え。本文には出さず、書くときの照合に使う。

- **清水軸（keep/tighten/drop/add）×扱い軸（課す/採らない）は直交**。継承（keep）を機械検査として課す（単一生成元）も、継承した不在を継続して採らない（条件分岐）もある。
  keep: 有形性(0001)・単一生成元(V001)・DAG・条件分岐の不在(0005)／tighten: 粒度4基準(0004)／drop: update(0003、清水軸で唯一の意味論変更)／add: テキスト DSL 化・tooling・`tags`(0019)・終端門番の運用側(0018)・最小コア＋拡張(0021)。
- **provenance スコープ（vs 清水 PFD）**: 清水の意味論に対し意図的に変更（drop）したのは update 不採用ただ一つ。他は継承（keep）か追加（add）。
  「追加」概念が PFDSL 内部で版を重ねて意味論を変える（`tags` の 0019 など）のは**内部版進化**で別軸。「PFDSL は自分の意味論を一度も変えていない」と誤読しないこと。
- **二層と二段は別レベル**: 第2部§2の「ルール対 機械検査」の二層（ADR-0006）と、「言語の `check` 対 運用側の点検」の二段（ADR-0018）は別で、後者は前者の機械検査の内訳。
  運用側の点検（終端ゲート・定期監査）は第3部§4で追加的に出す。第2部の機械検査が不完全だったと読ませない — 網を運用面へ広げる話であって覆す話ではない（誤読防止の接続を1文）。
- **採否**: ADR-0008（group 指針）は本連載から外す。ADR-0011（能力成果物の世代還流）は第2部で1文触れる程度。
  `schedule:` 全廃棄（ADR-0021）は第2部§3で任意・1文まで（実装を一周してから判定基準が効いた、の一例。主題化しない）。
- **2026-07-06 防波堤エントリの使い方**: 「監査機構自身を監査する再帰」の学習部分は内向き話なので本文に出さない。
  指摘の修理状況（#351/#352 は未解決）に触れず「分布の実証」までで切る。使用は第2部§2のみ（第3部の「監査できる」の傍証に使い回さない）。
- **有形性（ADR-0001）の置き場**: 第2部§2の機械検査の前提として1段落（出力は検証可能なモノ、入力は不定形可。「応急処置」→「暫定対応記録」。理解の外化は清水の XDDP の spec-out と同方向）。独立節にしない。

## ステータス・運用規約

- 進捗管理は `.pfdsl/roadmap.pfdsl` の `i12_article`（現 `todo`）。
- タイミング規約: status→done と issue #12 close は **main への PR マージ時**。公開（外部 URL 取得）はユーザー作業で公開後に done。
  feature branch では `closes #12` を使わない。
- 記事内の ` ```pfdsl ` コードブロックは `node scripts/check-doc-examples.mjs <md>` で検証。
- PR はユーザーが明示依頼するまで作らない。

## 次セッションへの申し送り

**現在地**:
- **R8 で構成を痛み階段に全面再設計、R9（2026-07-06）で旧第1部+旧第2部を統合し3部構成へ**（本 README が確定版）。
  R9 の理由: 旧第1部の初稿がユーザーレビューで「具体例はあるが情報として分かり切った話」と判定され、負例（前倒しモデリングの初期コスト）も却下された。
  新第1部は「既存のタスク管理（ToDo・WBS・DFD 等）はなぜ依存関係を表現できないか」を最初の焦点に据え、旧第2部のテキスト性・check 対比デモ・ドリフト負例までを1本で扱う。
  頂点の一文は R8 のまま。3条件の命名は第3部末（各部では名指ししない）。
- 全3部の本文ドラフトを commit 済み。第2部・第3部の初稿はユーザーレビューで「抽象的・体験談エクスキューズ過多・ポエミー」と却下され、R10 の規律で全面リライト（第1部も N=1 一文と体験談枠取りの除去・整形規範の適用で追随）。
  残作業: レビュー反映 → zenn-cli リポジトリ管理への移行（上記「公開先」）→ main への PR マージで status/issue 処理。
- 本文執筆時の判断: 「終端健全性」は対応する言語側検査が実在しないため第2部§2の検査リストから除外した（検査は単一生成元・循環・孤立プロセス・完了基準の4点）。
- R6 レビューの埋め戻しで本文に残るもの: 二層/二段の誤読防止1文（執筆者メモ参照）／第3部§3頭の N=1 射程一句／第2部§4の境界ケース→テスト生成は具体1例を必ず展開。

**執筆時の手順**（弱いモデル向け・機械的に従う）:
1. 対象部の節構成（本 README）とエピソード配分表を読む。
2. 各実話は `docs/pfd_payoff_log.md` の該当日付エントリを開いて一次情報から書く（この README の要約から書かない）。
3. 「書き方の規律」を照合してから提出（特に: 記号を本文に出さない・免責2点のみ・各部負例1件・用語の初出マップ順守）。
4. ` ```pfdsl ` ブロックは `node scripts/check-doc-examples.mjs <md>` を通す。
5. `.md` の改行は文境界のみ（CI `check-md-linebreaks`）。

**運用上の注意**:
- クローン直後は `make setup`（pre-commit hook 導入）。
- pfd-ops の pfd-cycle に従う（1サイクル＝1プロセス＝`write_article`）。コミットは Conventional Commits・英語。
