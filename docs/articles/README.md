# PFDSL 連載記事

ADR 群を素材に、PFDSL の問題意識と設計根拠を説明する公開記事（連載）。
issue [#12](https://github.com/takasek/pfdsl/issues/12) に対応し、リポの生態系図上は
`write_article` プロセスの出力 `i12_article` にあたる。

この README は連載の**方針と構成のインデックス**であり、執筆の前提を一箇所に固定するための文書。
各部の本文を書く前に、ここで軸・読者・NG・構成を確定させてから書く。

## 読者

エンジニア一般。プロセス（仕事の段取り）は職種を問わず誰もが日々扱うものなので、間口は広く取る。
「プロセス設計に関心のある人」には限定しない。PFD の前提知識は仮定せず、導入から書く。

## 軸（書くこと）

- **そもそもツール自体がない状態での問題**。
  プロセスを形式化したいのに、それを支え・制約を守らせる手段が存在しない、という状況そのものの問題。
- その**解としての PFDSL**。
- **制約をツールで管理することは重要な要求**。
  形式化は規約・人手レビューだけでは形骸化する。
  単一生成元・DAG 性・終端健全性などを機械が検査できる必要がある。

## 書かないこと（NG）

- 社内・SWET の GUI ツールとの差分・比較を**主題にしない**。
  要求の実在を示す先行例として一度だけ触れるに留める。
- 「作図ツールの限界」を記事の中核に据えない。
- AI 臭いポエム調・過剰な比喩・情緒的レトリックを使わない。**淡々と具体的に書く**。

## 言語

日本語で書く。これは CLAUDE.md「ユーザーの目に触れる公開ドキュメントは英語」への**意図的な例外**。
対象が日本の方法論（清水吉男の PFD）で、読者・媒体が日本語のため、明示的に日本語を選ぶ。
ファイル名・slug は英語。

## 配置・命名

- 本 README — 連載の方針と構成のインデックス。
- 各部の本文 — `docs/articles/NN-slug.md`（例: `01-tangible-text.md`）。`NN` は連番、`slug` は英語。

## 連載の構成（暫定・柔軟）

本数は固定せず、暫定 3〜4 部で柔軟に進める。第1部のみ確定、以降は暫定。

| 部 | 状態 | 責務 / テーゼ |
|---|---|---|
| 第1部 | **確定** | ツール不在の問題 → 要求の導出 → 解としての PFDSL。テーゼ: 成果物の明示・制約の機械検査・テキスト性が揃って初めて、検証可能でレビュー可能で AI と共有できるプロセス記述になる。 |
| 第2部 | 詰め中 | 制約の設計を「課す/採らない」で二分する。A群＝グラフを検証可能にするために**課す**制約、B群＝静的解析性と最小性を守るために**採らない**機能。下の節構成を参照。 |
| 第3部 | 保留 | 言語そのものの dogfooding（pfdsl）。次セッションで詰める。下の「第3・4部 アイデア」を参照。 |
| 第4部 | 保留 | 運用サイクル・ハーネスの dogfooding（pfd-cycle / pfd-ops / pfd-retro）と AI 協働。膨らむ見込みでさらに分割し得る。下の「第3・4部 アイデア」を参照。 |

## 第1部 節構成（確定）

仮タイトル: **「成果物を、テキストで」**（後で詰める）

1. **効能の動機（入口）** — 書き捨てでないプロジェクトには必ずタスクの依存関係が生じ、その管理が求められる。
   職種を問わず誰もが踏む状況として導入する。
2. **既存モデルの欠落** — 依存管理の代表は WBS。だが WBS は成果物を明示しないため、
   各タスクが「何を生んで完了したか」が検証不能になる。
   フローチャートも「順番は分かるが関連性の情報が希薄」（SWET 記事の指摘）。
3. **成果物中心モデル＝PFD** — 成果物を明示すれば完了が検証可能になる。これが清水吉男の PFD の核。
4. **制約のツール管理は実在要求で、先行例が満たしていた** — PFD の整合維持は人手では「かなりの労力」。
   SWET の PFD Draw Tool は単一生成元・二部性を「そもそも書けないようにする」ことで制約をツール強制した。
   比較が主題ではなく、要求の実在を示す先行例として一度だけ触れる。
5. **残った欠落＝生成 AI 時代の新要求** — Draw Tool はブラウザ上の手動編集が前提（生成 AI 以前）。
   テキスト表現を持たないため diff・レビュー・版管理（git）・AI への受け渡しができない。
6. **要求の導出（PFDSL）** — 制約の機械管理（先行例から継承）＋テキストベース（diff・レビュー・git・AI）
   ＋座標を持たずグラフ構造だけを編集＋メタデータの柔軟性。
7. **解：PFDSL** — README の最小例で記法を導入し、`check` で単一生成元などの制約検査を実演する。
8. **第1部テーゼ** — 上記が揃って初めて検証可能・レビュー可能・AI 共有可能なプロセス記述になる、と結ぶ。
   次部（各制約の設計判断）へ橋渡しする。

## 第2部 節構成（詰め中）— 制約の設計を「課す/採らない」で二分

**背骨テーゼ**: PFDSL は2方向で制約を選ぶ。(A) グラフが満たすべき性質を機械検査可能な形で**課す**。
(B) 静的解析性と最小性を壊す機能は**採らない**。両者を貫く判定基準は
「DAG の静的解析性（ランク計算・到達性・正準順序）を壊さないか」「機械が検査できるか」
「pfdsl 単体に価値があるか」。各 ADR はこの基準を別々の誘惑に適用した記録。

導入で第1部の要求（制約を機械が検査する）を受け、上記の判定基準を提示する。

**A群 — 課す制約（グラフを検証可能にする）**

| 節 | ADR | 設計問題 → 決定 | 裏づけ・例 |
|---|---|---|---|
| A1 成果物の有形性 | 0001 | 「理解」「合意」のような不定形を出力に許すか → 非対称規則（出力は検証可能なモノ／入力は不定形可） | 完了判定・status・引き継ぎが成立する条件。「応急処置」→「暫定対応記録」 |
| A2 単一生成元 | V001 | 同一成果物を複数プロセスが生成してよいか → 禁止 | 生成元の一意性が diff・改版・責任の基準点。Draw Tool も「書けないように」していた。0002/0003 の制約条件 |
| A3 終端の健全性 | 0018 | 出力ノードが宙に浮く／手段成果物が後続を欠く → 双方向門番 | 形骸化の構造的検出（第1部テーゼの回収）。spec_v008 が実装プロセスを欠いた例 |
| A4 プロセス粒度 | 0004 | どこまで分割するか → 4基準（時間的凝集禁止／新依存なし分割禁止／相互依存は1プロセス／所有権境界） | 偽依存と隠れ循環の排除。マイクロサービス同型。階層×プリセットが並列→上流方針→共同起草と収束 |

**B群 — 採らなかった機能（最小性と静的解析性を守る）**

| 節 | ADR | 欲しくなる場面 → 決定 | なぜ採らないか |
|---|---|---|---|
| B1 update 意味論 | 0003 | DB・本番・サービング中モデル → 不採用、スナップショット＋ビュー | update は循環を生み DAG 性・単一生成元・diff 安定性を壊す |
| B2 条件分岐 | 0005 | 合否・承認・閾値 → 不採用、成果物定義で解消 | 分岐は静的解析性を壊し状態機械と重複。承認記録・結果報告書＋`>>?` へ変換 |
| B3 subroutine | 0019 | 似た Process の再利用 → 却下、横断ラベル `tags` で代替 | 1:1 leveling から逸脱、図の現実地図性を損ない、境界チェックを複雑化（0004 と衝突） |
| B4 外部ツール専用フィールド | 0021 | 外部ツール連携（`schedule:`）→ 不採用 | 判定テスト＝グラフ意味論／可視化／`check` の本質検証のいずれかに寄与するか。spec の凝集と維持コスト/単体価値の非対称 |

**橋渡し — 改版の表現（0002）**: B の「update を採らない」と A の「単一生成元」を両立させると、
改版はどう書くか。3形態（別 artifact／`>>?` 収束／定常サイクル）。A と B の制約から演繹される
帰結として、B 群の後・結びの前に置く（位置は要確認）。

**結**: 課す/採らないは同じ基準の表裏。各 ADR はその基準を別々の誘惑に適用した記録。第3部（自己適用）へ橋渡し。

採否メモ: ADR-0008（存在様式でグループを切る）は `group` 用法の指針で「課す/採らない」制約ではないため第2部から外す。
ADR-0011（能力成果物の世代還流 `>>?`）は自己適用と強く結びつくため第3部の候補とする。

## 第3・4部 アイデア（保留・随時追記）

確定ではない。次セッション以降に詰めるための着想置き場。

**第3部 — 言語そのものの dogfooding（pfdsl）**
- テーゼ案: 方法論を自分の開発に適用したら、設計判断器・テスト生成器・レビュー装置として働いた。
- 素材（`docs/pfd_payoff_log.md`）: 着手可能集合の機械導出／暗黙依存のレビュー可能化／
  相互依存の先回りモデル化（並列主張の罠回避）／ADR の具体例がそのまま TDD fixture／W002 拡張が自リポを直撃。
- ADR-0011（能力成果物の世代還流）はここで扱う候補。

**第4部 — 運用サイクル・ハーネスの dogfooding と AI 協働（pfd-cycle / pfd-ops / pfd-retro）**
- テーゼ案: 制約をテキストで機械検査できるから、AI エージェントが文脈ゼロでも運用に参加でき、
  失敗様式が「構造的欠落」として検出できる。
- 素材: cold subagent での dogfood→検出→改訂→再検証の閉ループ／retro C層が運用ルールの暗黙スコープを欠落として検出／
  lint 要件経路の完結。
- **ハーネス自体の自己改善**: pfd-retro の効力＝気付き依存を構造依存へ移管する。
- **L3 をカスタマイズ可能にしている設計**: 運用バインディングを companion／references の差し替えで効かせる層構成。
- AI 意思疎通言語の角度はここで展開（第1部では軽く触れる程度に留める）。
- 膨らむ見込み。第4部はさらに分割し得る前提で保留。

## 素材

- `docs/adr/`（特に 0001〜0006 が言語設計の核）
- `docs/pfd_payoff_log.md`（PFD が効いた実エピソード集 — 効能の裏づけ）
- `README.md`、`docs/spec/spec.md`
- 背景の参考: SWET ブログ2本（本文で軽く一度触れる。比較はしない）
  - https://swet.dena.com/entry/2024/11/15/120000
  - https://swet.dena.com/entry/2026/01/16/120000

## ステータス・運用規約

- 進捗管理は `.pfdsl/roadmap.pfdsl` の `i12_article`（現 `todo`）。
- タイミング規約: `i12_article` の status→done と issue #12 close は **main への PR マージ時**。
  公開（外部 URL 取得）はユーザー作業で、公開後に done。feature branch では `closes #12` を使わない。
- 記事内の ` ```pfdsl ` コードブロックは `node scripts/check-doc-examples.mjs <md>` で検証する。
- PR はユーザーが明示依頼するまで作らない。
