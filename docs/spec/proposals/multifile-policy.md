# マルチファイル意味論 共有決定記録

## 対象仕様バージョン

v0.0.7 → v0.0.8

## 概要

#5（階層 PFD）と #6（共有プリセット）は、複数の `.pfdsl` ファイルにまたがる参照・継承を導入する。両提案が共通前提とするファイル間意味論（ID スコープ・参照形式・継承解決順）を本文書に決定記録として集約する。`integrate_multifile` プロセスの入力成果物のひとつ。

---

## 決定 1: ID スコープ = ファイルローカル

### 決定

各 `.pfdsl` ファイルは独立した ID 名前空間を持つ。artifact ID および process ID はファイル内でのみ一意であればよい。グローバルレジストリは持たない。

### 根拠

- **衝突回避**: 異なるファイルが同名 ID を持っていても衝突しない。大規模プロジェクトで別々の作者が独立してファイルを書ける。
- **単一生成元制約の保全**: 既存制約 V001（各 artifact は高々 1 つのプロセスにより生成）/ V002 / V003 はファイル単位で成立すれば十分。クロスファイルに拡大すると、どのファイルが生成元かを追跡するグローバルグラフが必要になり実装コストに見合わない。
- **境界が契約として可視化される**: ファイル間の依存は後述の明示的参照構文でのみ表現される。暗黙的なグローバル名前空間は依存関係を不可視にする。

---

## 決定 2: ファイル間参照 = 明示的・相対パスベース＋境界 ID 協定

### 決定

#### 2a. 参照形式

ファイル間参照は常に相対パスで記述する。パス解決の基準は「含む `.pfdsl` ファイルの位置」とする（i13-location.md の `location:` パス解決規則と一貫）。

**#5 階層 PFD の参照構文**（プロセス専用フィールド）:

```yaml
process:
  x:
    subflow: ./sub.pfdsl
```

**#6 共有プリセットの参照構文**（トップレベル frontmatter キー）:

```yaml
extends: ./presets.yaml
```

絶対パスおよびリポジトリルート相対パスは使用しない。URL（`://` 形式）も subflow/extends の値としては不可。

#### 2b. 境界 ID 協定（#5 のみ）

ファイルローカル名前空間を維持しつつ、親子フローの整合を検証するため、「境界 artifact にかぎり親子で同一 ID を共有する」を契約とする。

- 子フローの **open input artifact**（生成元プロセスなし）は、親プロセスの入力エッジが指す artifact と同一 ID でなければならない。
- 子フローの **terminal artifact**（消費先プロセスなし）は、親プロセスの出力エッジが指す artifact と同一 ID でなければならない。
- 内部（非境界）の artifact / process ID は各ファイルで自由に定義できる。
- checker は `subflow:` を持つプロセスを見つけた場合、境界整合を検証する。

#### 2c. V001 との関係

`subflow:` はビューリンクであり生成の複製ではない。親プロセスは子フローの「要約ビュー」として機能し、子フロー内で境界 artifact を再生成しても別ファイル（別名前空間）に属するため V001 違反にならない。

### 根拠

- **相対パス固定**: `location:` フィールドが採用した「含む `.pfdsl` ファイルからの相対パス」規則（i13-location.md）を踏襲し、ファイルを移動する際の変更範囲をローカルに保つ。
- **境界 ID 協定**: ファイルローカル名前空間のまま境界整合を機械検証できる最小の規約。グローバルレジストリを導入せずに Shimizu PFD の階層整合性を保証する。
- **V001 非侵犯**: subflow をビューリンクと定義することで、既存の単一生成元制約を崩さずに階層表現を追加できる。

---

## 決定 3: 継承解決順 = キー単位マージ・nearest-wins

### 決定

`extends:` の継承は以下の規則で解決する。

1. **対象スコープ**: `extends:` で共有できるのは presentation 系トップレベル frontmatter キー（`statusStyles` / `tag` / `group`）のみ。`artifact` / `process` 定義およびボディのエッジ文は共有しない。
2. **キー単位マージ**: ファイルローカル定義が prevail。同一キーはローカルが上書き、衝突しないキーは union される（ブロック丸ごと置換ではない）。
3. **多段 extends**: preset が別の preset を `extends:` することを許可する。解決は深さ優先・最近傍勝ち（nearest-wins）。`A extends B extends C` の場合、同一キーでは A > B > C の優先順。
4. **循環参照禁止**: `A extends B, B extends A` は error。checker が検証する。

### 根拠

- **artifact/process 非共有**: ファイル単位の単一生成元原則（決定 1）を保つため、生成物の定義はファイルをまたいで共有しない。statusStyles / tag / group のみが純粋な見た目（およびタグ定義）の設定であり共有コストゼロ。
- **キー単位マージ**: ブロック丸ごと置換にするとプリセットの一部キーだけ上書きしたい場合に全量再定義が必要になる。キー単位マージはローカル変更を最小にする。
- **nearest-wins**: 直感的で予測可能。ローカル定義（最近傍）が常に勝つため、プリセット変更がローカルの意図を意図せず上書きしない。

---

## #5・#6 提案および integrate_multifile との関係

| 成果物 | 依存する本文書の決定 |
|--------|---------------------|
| i5_hierarchy_spec（#5） | 決定 1（ファイルローカル名前空間）・決定 2（subflow 構文・境界 ID 協定・V001 関係） |
| i6_presets_spec（#6） | 決定 1・決定 3（extends 構文・継承解決順・対象スコープ） |
| integrate_multifile → spec_v008 | 本文書全決定 + i52_featured_toolchain 完了後に統合（ADR-0013） |

本文書の起草は `spec_v006` を入力として先行可能（ADR-0013 §Consequences）。`integrate_multifile` プロセス自体は `i52_featured_toolchain` 完了まで着手不可。v0.0.7 実装中に `location:` / `revises:` のクロスファイル意味論に関する設計判断が生じた場合は ADR 化して本文書へ反映する経路が存在する（ADR-0013 §Consequences）。
